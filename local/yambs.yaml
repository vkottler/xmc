---
includes:
  - ../config/python/yambs/cflag_groups.yaml

default_target: xmc

ldflag_groups:
  xmc: [-Tsrc/link.ld]

variants:
  xmc:
    prefix: "arm-picolibc-eabi-"
    cflag_groups: [picolib, m4, hwfp, armv7emfp]
